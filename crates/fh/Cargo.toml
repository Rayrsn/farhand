[package]
name = "fh"
description = "Farhand CLI client: offload compilation, test, and build jobs to remote machines"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
protocol = { version = "1.8", path = "../protocol" }
fileset = { version = "1.8", path = "../fileset" }
config = { version = "1.8", path = "../config" }
templates = { version = "1.8", path = "../templates" }
tokio = { workspace = true, features = ["full"] }
clap = { workspace = true, features = ["derive", "env"] }
clap_complete.workspace = true
clap_mangen.workspace = true
tracing.workspace = true
tracing-subscriber = { workspace = true, features = ["env-filter", "json"] }
serde_json = { workspace = true }
futures = { workspace = true }
crossterm = { workspace = true }
notify = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
fhd = { version = "1.8", path = "../fhd" }
workspace = { version = "1.8", path = "../workspace" }
tempfile = { workspace = true }
# Only for the process-liveness probe in the cancellation e2e test
# (signal 0); already in the dependency graph via fhd/workspace.
libc = { workspace = true }
